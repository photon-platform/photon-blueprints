form:
  validation: loose
  fields:
    section:
        type: section
        title: Add Person

    title:
      type: text
      label: Person Full Name (title)
      validate:
        required: true

    folder:
      type: hidden
      default: '@slugify-title'

    route:
      type: hidden
      default: /

    name:
      type: hidden
      default: 'person'

    visible:
      type: hidden
      default: ''

    blueprint:
      type: blueprint
